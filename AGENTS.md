# nixos-hyprland guide

Keep stable Hyprland and Dank Material Shell configuration in this repository.
Do not add `config/dms/session.json`. DMS owns that private runtime state under `~/.local/state`.

Format the repository with `nix fmt`.
Validate it with `nix flake check --all-systems`.
